
    let cssVar=(k:string)=> "var(--"++k++")"
    // Module contents
    let colors = {
        "background-grey":cssVar("background-grey"),
        "primary-white":cssVar("primary-white"),
        "primary-shadow":cssVar("primary-shadow")
    }
